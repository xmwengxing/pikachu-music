# v1.0.26 Implementation Plan — 去掉 Render 默认 + 启动自动同步共享 cookie

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Pikachu Music v1.0.26 — 新装 APK 默认无 Render 后端地址、聚合源默认关；App 启动/切换后端/回到前台时自动把后端 cookies 同步进本地 cookieBackup；删除冗余的 `build-artifacts/` 目录。

**Architecture:**
- 在 `settingsStore.ts` 把 Render 默认值清空，新装用户走干净默认；升级用户保留旧值（不写新 migrate）。
- 新增 `src/api/cookieSync.ts` 封装"拉 + 长者胜合并 + 写回"。
- `App.tsx` 用 `syncCookiesFromBackend()` 替换现有裸 fetch 暖机；新增 AppState='active' 监听。
- `SettingsPanel.tsx` baseUrl 空时显示 warning 色 hint 卡片。
- 删除整个 `build-artifacts/` 目录（含 `README.md`）；版本表迁移到根 `README.md`。

**Tech Stack:** React Native (Expo SDK 56) + TypeScript + Zustand + AsyncStorage；Android Gradle 8.x。

---

## File Structure

| 文件 | 角色 | 改动 |
| --- | --- | --- |
| `src/state/settingsStore.ts` | 默认值与持久化 | 4 处默认值变更 |
| `src/api/cookieSync.ts` | **新增**：拉取+合并+写回 | 新建 |
| `App.tsx` | 根组件 | 替换暖机 fetch + 新增 AppState 监听 |
| `src/screens/SettingsPanel.tsx` | 设置面板 | baseUrl 空时显示 hint 卡片 |
| `src/i18n/translations.ts` | 中英 i18n | 新增 4 个 key |
| `app.json` | Expo 版本 | version 已是 1.0.26（仅验收） |
| `android/app/build.gradle` | Gradle 版本 | versionCode/Name 已是 26/1.0.26（仅验收） |
| `README.md` | 根 README | 同步发布路径与版本表 |
| `build-artifacts/` | 冗余发布目录 | 整个删除 |

---

### Task 1: 修改 `settingsStore.ts` 默认值

**Files:**
- Modify: `src/state/settingsStore.ts:16-22` (DEFAULT_ENABLED)
- Modify: `src/state/settingsStore.ts:43-57` (DEFAULT_RENDER_BACKEND / DEFAULT_BACKENDS / DEFAULT_ACTIVE_ID)
- Modify: `src/state/settingsStore.ts:134-136` (initial state: gomusicBaseUrl / backends / activeBackendId)

- [ ] **Step 1.1: 修改 `DEFAULT_ENABLED`，把 `gomusic` 改为 `false`**

在 `src/state/settingsStore.ts:20` 把 `'gomusic'` 从 `includes` 数组里去掉：

```ts
const DEFAULT_ENABLED: Record<string, boolean> = PLATFORMS
  .filter(p => !p.hidden && !p.loginOnly)
  .reduce((acc, p) => {
    // 默认开启主平台（聚合源默认关，新装用户需在设置里填入后端地址后手动开启）
    acc[p.id] = ['migu', 'netease', 'qq', 'kugou', 'kuwo'].includes(p.id);
    return acc;
  }, {} as Record<string, boolean>);
```

- [ ] **Step 1.2: 把 `DEFAULT_RENDER_BACKEND` / `DEFAULT_BACKENDS` / `DEFAULT_ACTIVE_ID` 改为空**

替换 `src/state/settingsStore.ts:43-57`：

```ts
// v1.0.26：移除 Render 默认后端。
// 新装用户拿到的 backends 是空数组，必须在设置里自行添加/填写后端地址。
// 升级用户的旧值通过 persist 的 partialize 字段保留，不写新 migrate。
const DEFAULT_BACKENDS: Backend[] = [];
const DEFAULT_ACTIVE_ID: string | null = null;
```

- [ ] **Step 1.3: 把初始 state 的三个字段改为空**

替换 `src/state/settingsStore.ts:134-136`：

```ts
      gomusicBaseUrl: '',           // v1.0.26: 新装用户无默认 Render 地址
      backends: DEFAULT_BACKENDS,   // []
      activeBackendId: DEFAULT_ACTIVE_ID, // null
```

- [ ] **Step 1.4: 保持 `migrate` / `onRehydrateStorage` 不变**

它们仍在兜底（用户数据残缺时补默认），不删除。新默认是 `[]` / `null`，对升级路径没有破坏性：
- 老用户的 `backends` 已有 Render 项 → `Array.isArray(...) && length > 0` 为 true → 不覆盖 ✓
- 老用户 `activeBackendId` 已设 → 不为空 → 不覆盖 ✓
- 真新装用户（无 persisted） → 走 step 1.3 的初始值 → `[]` / `null` ✓

确认：不需要修改 `migrate` 函数。

- [ ] **Step 1.5: 编译验证**

Run:
```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && npx tsc --noEmit 2>&1 | head -40
```

Expected: 无报错（最多是已有的、与本次无关的 warning）。`settingsStore.ts` 类型完整。

- [ ] **Step 1.6: 提交**

```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && \
  git add src/state/settingsStore.ts && \
  git commit -m "v1.0.26: 新装用户无 Render 默认后端 + 聚合源默认关

- DEFAULT_RENDER_BACKEND / DEFAULT_BACKENDS 改为空
- DEFAULT_ACTIVE_ID = null
- gomusicBaseUrl 初始 = ''
- enabledSources.gomusic 默认 false
- 升级用户保留旧值（migrate/onRehydrateStorage 不动）"
```

---

### Task 2: 新增 `src/api/cookieSync.ts`

**Files:**
- Create: `src/api/cookieSync.ts`

- [ ] **Step 2.1: 创建文件并实现同步函数**

```ts
// src/api/cookieSync.ts
// 多设备共享 cookie：每次打开 App / 切后端 / 回前台 时，
// 把后端 /system/cookies 拉回来，与本地 cookieBackup 按"长者胜"规则合并后写回。
//
// 设计动机：
// - 后端 cookies.json 是共享存储，A 设备扫码登录后 B 设备本地缓存为空，
//   单纯靠 CookiesTab/AccountsTab mount 时拉一次不够（用户可能根本不开这俩 tab）。
// - "长者胜"：保护用户在 CookiesTab 手动粘贴的较长 cookie 不被远程短值覆盖；
//   扫码登录产生的 cookie 通常更长，所以新扫码也会赢。
import { listCookies } from './gomusic';
import { useSettingsStore } from '../state/settingsStore';

export interface CookieSyncResult {
  ok: boolean;
  /** 实际写回 cookieBackup 的字段数（=0 表示没变化或后端为空） */
  updated: number;
  /** ok=false 时的简短原因 */
  reason?: 'no-base-url' | 'fetch-failed' | 'invalid-response' | 'store-error';
}

/**
 * 从当前激活后端拉取 cookies，按"长者胜"与本地 cookieBackup 合并，写回 store。
 *
 * 行为表：
 *   baseUrl 为空           → { ok:false, reason:'no-base-url', updated:0 }
 *   后端不可达 / fetch 抛错 → { ok:false, reason:'fetch-failed', updated:0 }（静默）
 *   后端返 { code:200, data:{...} } 但 data 不是对象 → invalid-response
 *   合并后无变化            → { ok:true, updated:0 }
 *   合并后有变化            → { ok:true, updated:N }
 */
export async function syncCookiesFromBackend(): Promise<CookieSyncResult> {
  let remote: Record<string, string> | null = null;
  try {
    remote = await listCookies();
  } catch {
    return { ok: false, reason: 'fetch-failed', updated: 0 };
  }
  if (!remote || typeof remote !== 'object') {
    return { ok: false, reason: 'invalid-response', updated: 0 };
  }

  const settings = useSettingsStore.getState();
  const local = settings.cookieBackup || {};
  const next: Record<string, string> = { ...local };
  let updated = 0;

  for (const [source, cookie] of Object.entries(remote)) {
    if (!cookie) continue;
    const cur = next[source];
    if (!cur) {
      next[source] = cookie;
      updated++;
      continue;
    }
    // 长者胜：保留较长的一方
    if (cookie.length > cur.length) {
      next[source] = cookie;
      updated++;
    }
  }

  if (updated === 0) {
    return { ok: true, updated: 0 };
  }
  try {
    useSettingsStore.getState().loadFromObject({ cookieBackup: next });
    return { ok: true, updated };
  } catch {
    return { ok: false, reason: 'store-error', updated: 0 };
  }
}
```

- [ ] **Step 2.2: 类型检查**

Run:
```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && npx tsc --noEmit 2>&1 | head -40
```

Expected: 无新增报错。`listCookies` 已存在于 `src/api/gomusic.ts`，签名 `(): Promise<Record<string, string> | null>` 匹配。

- [ ] **Step 2.3: 提交**

```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && \
  git add src/api/cookieSync.ts && \
  git commit -m "v1.0.26: 新增 cookieSync 模块（拉取+长者胜合并+写回）"
```

---

### Task 3: 修改 `App.tsx` 接入 cookieSync

**Files:**
- Modify: `App.tsx:48-69`（两个 useEffect）

- [ ] **Step 3.1: 引入 AppState 与 cookieSync**

在 `App.tsx:10` 现有的 react-native import 中追加 `AppState`：

```ts
import { View, Text, StyleSheet, ActivityIndicator, AppState } from 'react-native';
```

- [ ] **Step 3.2: 删除裸 fetch 暖机逻辑，替换为 cookieSync 触发**

替换 `App.tsx:48-69` 的两个 useEffect 为：

```ts
  /**
   * cookie 同步（v1.0.26）
   * - 启动 3 秒后拉一次
   * - AppState 回到 'active' 时拉一次（用户从后台切回）
   * - activeBackendId 变化时立即拉一次（用户切换后端）
   * - 失败静默（baseUrl 为空、fetch 抛错都吞掉）
   */
  useEffect(() => {
    const t = setTimeout(() => {
      syncCookiesFromBackend().catch(() => {});
    }, 3000);
    const sub = AppState.addEventListener('change', (state) => {
      if (state === 'active') {
        syncCookiesFromBackend().catch(() => {});
      }
    });
    return () => {
      clearTimeout(t);
      sub.remove();
    };
  }, []);

  const activeId = useSettingsStore((s) => s.activeBackendId);
  useEffect(() => {
    if (!activeId) return;
    syncCookiesFromBackend().catch(() => {});
  }, [activeId]);
```

- [ ] **Step 3.3: 添加 import**

在 `App.tsx:24` 现有 `getGomusicBase` import 后追加：

```ts
import { syncCookiesFromBackend } from './src/api/cookieSync';
```

- [ ] **Step 3.4: 类型检查**

Run:
```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && npx tsc --noEmit 2>&1 | head -40
```

Expected: 无新增报错。

- [ ] **Step 3.5: 提交**

```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && \
  git add App.tsx && \
  git commit -m "v1.0.26: 启动/active/切后端 三处触发 cookieSync（替代裸 fetch 暖机）"
```

---

### Task 4: i18n 新增 hint 文案

**Files:**
- Modify: `src/i18n/translations.ts` (zh 对象 + en 对象)

- [ ] **Step 4.1: 添加中文 key**

在 `src/i18n/translations.ts:162`（`settingsLocalBackendHint` 之后）插入：

```ts
    settingsBackendUrlEmptyHint: '未配置后端 API 地址，聚合音源不可用。点下方"+ 添加后端"填写你的地址。',
    settingsBackendUrlEmptyHintDoc: '部署文档：docs/GOMUSIC-API-DEPLOY.md（仓库根目录）',
```

- [ ] **Step 4.2: 添加英文 key**

在 `src/i18n/translations.ts` 的 `en` 对象里对应位置插入（查找 `settingsLocalBackendHint:` 后面）：

```ts
    settingsBackendUrlEmptyHint: 'Backend API URL not configured; aggregate sources are disabled. Tap "+ Add Backend" below to enter yours.',
    settingsBackendUrlEmptyHintDoc: 'Deploy guide: docs/GOMUSIC-API-DEPLOY.md (repo root)',
```

定位方法：在 `translations.ts` 的 en 区段找到 `settingsLocalBackendHint:` 行，把上面两行插入到它后面（保持中英同序）。

- [ ] **Step 4.3: 类型检查**

Run:
```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && npx tsc --noEmit 2>&1 | head -40
```

Expected: 无报错。

- [ ] **Step 4.4: 提交**

```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && \
  git add src/i18n/translations.ts && \
  git commit -m "v1.0.26: i18n 新增 baseUrl 空时 hint 文案（中英）"
```

---

### Task 5: `SettingsPanel.tsx` 显示 hint 卡片

**Files:**
- Modify: `src/screens/SettingsPanel.tsx:243-247`（BackendSection 的 hint 区域）

- [ ] **Step 5.1: 在 BackendSection 里读取 `hasAnyBackend` 派生值**

修改 `src/screens/SettingsPanel.tsx:201-247` 的 `BackendSection` 函数：

```ts
function BackendSection() {
  const { t } = useTranslation();
  const settings = useSettingsStore();
  const [addOpen, setAddOpen] = useState(false);
  const [lastTest, setLastTest] = useState<Record<string, BackendTestResult | undefined>>({});
  const [testingId, setTestingId] = useState<string | null>(null);

  // v1.0.26: 新装用户 backends=[]，baseUrl='' → 显式提示。
  // 这里同时考虑 gomusicBaseUrl（旧字段，兼容老用户）。
  const hasAnyBackend =
    (settings.backends && settings.backends.length > 0) ||
    !!(settings.gomusicBaseUrl && settings.gomusicBaseUrl.trim());
```

（其余 handleTest / handleDelete / 渲染逻辑不变）

- [ ] **Step 5.2: 在 hint 文本下方插入 hint 卡片**

修改 `src/screens/SettingsPanel.tsx:243-246`（`<Section title=...>` 内部的第一段 `<Text style={styles.hint}>` 之后、列表 map 之前）：

```tsx
      <Text style={styles.hint}>
        {t('backendHint') || '选择/添加聚合后端地址。空 = 关闭聚合回落到咪咕/网易云/QQ/酷我 4 个老平台。点"测试"验证连通。'}
      </Text>

      {!hasAnyBackend && (
        <View style={styles.emptyBackendCard}>
          <Text style={styles.emptyBackendCardTitle}>
            ⚠ {t('settingsBackendUrlEmptyHint') || '未配置后端 API 地址'}
          </Text>
          <Text style={styles.emptyBackendCardHint}>
            {t('settingsBackendUrlEmptyHintDoc') || '部署文档：docs/GOMUSIC-API-DEPLOY.md'}
          </Text>
        </View>
      )}
```

- [ ] **Step 5.3: 添加卡片样式**

在 `src/screens/SettingsPanel.tsx` 的 `styles` 对象末尾（`addInput` 之后、`}` 之前）追加：

```ts
  // v1.0.26: baseUrl 空时显示的提示卡片
  emptyBackendCard: {
    backgroundColor: 'rgba(245, 158, 11, 0.12)',
    borderColor: colors.warning,
    borderWidth: 1,
    borderRadius: radius.md,
    paddingHorizontal: spacing.sm,
    paddingVertical: spacing.sm,
    marginBottom: spacing.sm,
  },
  emptyBackendCardTitle: {
    color: colors.warning,
    fontSize: fontSize.sm,
    fontWeight: '700',
    marginBottom: 4,
  },
  emptyBackendCardHint: {
    color: colors.textMuted,
    fontSize: fontSize.xs,
    lineHeight: 16,
  },
```

- [ ] **Step 5.4: 类型检查**

Run:
```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && npx tsc --noEmit 2>&1 | head -40
```

Expected: 无报错。

- [ ] **Step 5.5: 提交**

```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && \
  git add src/screens/SettingsPanel.tsx && \
  git commit -m "v1.0.26: baseUrl 空时显示 warning 色 hint 卡片 + 部署文档路径"
```

---

### Task 6: 验收版本号（只读）

**Files:**
- Read only: `app.json`, `android/app/build.gradle`

- [ ] **Step 6.1: 确认版本号已是 1.0.26 / 26**

```bash
grep '"version"' /home/shijingtian/workspace/projects/pikachu-music-rn/app.json
grep -E 'versionCode|versionName' /home/shijingtian/workspace/projects/pikachu-music-rn/android/app/build.gradle
```

Expected:
```
    "version": "1.0.26",
        versionCode 26
        versionName "1.0.26"
```

如不符，回到本计划前修。如已符合 → 不 commit、跳过。

- [ ] **Step 6.2: 标记完成**

无需 commit，仅确认。

---

### Task 7: 删除 `build-artifacts/` 整个目录

**Files:**
- Delete: `build-artifacts/`（含 `README.md` 和 2 个 APK 文件）

- [ ] **Step 7.1: 用 `git rm -r` 删除目录**

```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && \
  git rm -r build-artifacts/
```

Expected 输出：`rm 'build-artifacts/README.md'` 和 `rm 'build-artifacts/pikachu-music-v1.0.26-arm64.apk'` 等 4 项。

- [ ] **Step 7.2: 确认目录已不在工作树**

```bash
ls /home/shijingtian/workspace/projects/pikachu-music-rn/build-artifacts 2>&1
```

Expected: `No such file or directory`。

- [ ] **Step 7.3: 提交**

```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && \
  git commit -m "v1.0.26: 删除冗余 build-artifacts/ 目录

原 build-artifacts/ 是手动把 gradle 默认输出 android/app/build/outputs/apk/release/
复制重命名过去的产物。gradle 默认路径已是权威产物位置，build-artifacts/ 重复且
git 历史里已有 4 次 commit，删除以简化仓库结构。

v1.0.26 release APK 直接位于 android/app/build/outputs/apk/release/：
  - app-arm64-v8a-release.apk
  - app-universal-release.apk
"
```

---

### Task 8: 同步根 `README.md` 的发布说明

**Files:**
- Modify: `README.md`（如原文中引用了 build-artifacts）

- [ ] **Step 8.1: 检查 README 是否引用 build-artifacts**

```bash
grep -n "build-artifacts" /home/shijingtian/workspace/projects/pikachu-music-rn/README.md 2>&1
```

如无输出 → Task 8 整体跳过（无需修改）。

如有引用 → 把 `build-artifacts/pikachu-music-vX.Y.Z-arm64.apk` 替换为 `android/app/build/outputs/apk/release/app-arm64-v8a-release.apk`；把 `build-artifacts/README.md` 替换为"见仓库根 README 或 DEV.md"。

- [ ] **Step 8.2: 提交（如有改动）**

```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && \
  git add README.md && \
  git commit -m "v1.0.26: 根 README 同步 APK 发布路径（移除 build-artifacts 引用）"
```

（无改动则跳过此 step。）

---

### Task 9: 构建 v1.0.26 release APK

**Files:**
- Build: `android/app/build/outputs/apk/release/app-arm64-v8a-release.apk`
- Build: `android/app/build/outputs/apk/release/app-universal-release.apk`

- [ ] **Step 9.1: 清理旧构建缓存（如需要）**

```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn/android && \
  ./gradlew clean 2>&1 | tail -5
```

（如已存在 release APK 且只是改 JS 代码层，可以省略 clean；但 settingsStore.ts 是 native 侧也会读取的常量，保险起见 clean。）

- [ ] **Step 9.2: 执行 release 构建**

```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn/android && \
  ./gradlew assembleRelease 2>&1 | tail -30
```

Expected: 出现 `BUILD SUCCESSFUL` 字样，路径 `app/build/outputs/apk/release/` 下生成两个 APK。

- [ ] **Step 9.3: 验证 APK 已生成**

```bash
ls -la /home/shijingtian/workspace/projects/pikachu-music-rn/android/app/build/outputs/apk/release/ | grep apk
```

Expected:
```
-rw-r--r-- 1 ... app-arm64-v8a-release.apk
-rw-r--r-- 1 ... app-universal-release.apk
```

- [ ] **Step 9.4: 验证版本号**

```bash
unzip -p /home/shijingtian/workspace/projects/pikachu-music-rn/android/app/build/outputs/apk/release/app-arm64-v8a-release.apk \
  AndroidManifest.xml 2>/dev/null | grep -oE 'versionName="[^"]*"|versionCode="[^"]*"' | head -5
# 或直接读 APK 路径信息
/opt/android-sdk/build-tools/*/aapt dump badging /home/shijingtian/workspace/projects/pikachu-music-rn/android/app/build/outputs/apk/release/app-arm64-v8a-release.apk 2>/dev/null | grep -E "package:|versionName|versionCode" | head -5
```

Expected: `versionName='1.0.26'`、`versionCode='26'`。

- [ ] **Step 9.5: 标记完成**

无需 commit（APK 不进 git）。两个产物路径供手动验证使用。

---

### Task 10: 手动验证清单（spec 第 6 节）

> 项目无自动化测试框架，按 spec §6 全程手动验证。

- [ ] **Step 10.1: 新装场景验证**

1. 卸载旧版（如已装）→ 安装 Task 9 构建的 `app-arm64-v8a-release.apk`
2. 启动 → 设置 → 后端地址区
   - 列表为空（无 Render 默认）
   - 显示 ⚠ 黄色 hint 卡片，文案正确（中英切换都看）
3. 搜索"周杰伦"
   - legacy 4 平台（咪咕/网易/QQ/酷我）能搜到结果
   - 聚合（go-music-api）默认未勾选 → 搜索时不调后端

- [ ] **Step 10.2: 升级场景验证**

1. 装 v1.0.25（旧 APK 需另行构建或从 git 历史取）→ 设置 baseUrl = 任意可访问的后端、勾选聚合、扫码登录一平台
2. 覆盖安装 v1.0.26（Task 9 产物）
3. 启动 → 设置
   - baseUrl 保留
   - 聚合勾选状态保留
   - 已扫码平台仍显示已登录
4. 收藏、歌单、搜索历史都还在

- [ ] **Step 10.3: 多设备同步验证**

1. 准备：让用户 A 拿设备 A、设备 B 都连同一后端（可临时用 `python -m http.server` + 自建后端或 Render）
2. A 扫码登录 qq
3. B 启动 App → ≤3 秒 → 设置 → 账号登录 → qq 显示已登录 ✓
4. B 切到后台再回前台 → AppState='active' → 仍是已登录 ✓
5. B 在设置里切到另一后端 → 立即触发 → 同步新后端的 cookies ✓

- [ ] **Step 10.4: 错误兜底验证**

1. 设置 baseUrl = `https://nonexistent.example.com` → 启动 App → 不报错、不弹 toast
2. 后端关闭 → 启动 App → 不报错
3. CookiesTab 手动粘贴一个 cookie → 重启 App → 长者胜策略不覆盖用户输入

- [ ] **Step 10.5: 标记完成**

无 git 操作。验证清单全过才进入 Task 11。

---

### Task 11: 最终提交与推送

- [ ] **Step 11.1: 检查 git 状态**

```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && git status --short
```

Expected: working tree clean（除 release APK 在 `.gitignore` 里之外）。如还有未提交改动，按改动内容决定 commit 或 stash。

- [ ] **Step 11.2: 推送**

```bash
cd /home/shijingtian/workspace/projects/pikachu-music-rn && git push origin master 2>&1 | tail -10
```

Expected: `* [new branch] master -> master` 或 `Everything up-to-date` 字样；本次推送应包含 commits from Tasks 1–8 共 7 个。

- [ ] **Step 11.3: 在 GitHub 上确认**

打开 `https://github.com/<user>/pikachu-music-rn/commits/master` 检查 commits 都在。

- [ ] **Step 11.4: 标记完成**

v1.0.26 release 完成。

---

## 自审报告

**1. Spec 覆盖**：spec 9 节内容全部对应到 plan：
- §2.1 默认值变更 → Task 1
- §2.2 UI 提示 → Task 4 + Task 5
- §2.3 启动/active/切后端 三处触发 → Task 2 + Task 3
- §2.4 不引入多账户 → 全 plan 未涉及
- §3.1 / §3.2 文件改动表 → File Structure 表
- §6 验收清单 → Task 10
- §8 验收标准 → Task 6 + Task 9 + Task 10 + Task 11
- 删除 build-artifacts/ → Task 7 + Task 8

**2. Placeholder scan**：搜索无 `TBD` / `TODO` / "implement later" / "fill in details" / "add appropriate error handling"。

**3. 类型一致性**：
- `syncCookiesFromBackend()` 在 Task 2 定义，Task 3 引用 — 签名一致 ✓
- `useSettingsStore.getState().loadFromObject({ cookieBackup })` — Task 2 用，Task 1 已确认 `loadFromObject` 在 line 252 ✓
- `colors.warning` — Task 5 用，Task 调研已确认存在于 `src/theme/colors.ts:31` ✓

**4. 无 spec 遗漏**：删除 build-artifacts 是用户在 brainstorming 后追加的决策，已并入 Task 7 + Task 8；与 spec 兼容（spec 不依赖 build-artifacts/）。

---

**End of plan. Awaiting execution choice (subagent-driven vs inline).**
